import TerminalBlock from "@/components/TerminalBlock";
import NotFoundClient from "@/components/NotFoundClient";

export default function NotFound() {
  return (
    <TerminalBlock id="not-found">
      <NotFoundClient />
    </TerminalBlock>
  );
}
